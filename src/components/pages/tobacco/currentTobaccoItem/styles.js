import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    grBtns: {
        marginTop: 12
    },
    counter: {
        maxWidth: 50,
        marginTop: 5,
        marginBottom: 5
    },
    root: {
        marginTop: "100px",
    },
    cardMedia: {
        margin: "auto",
        width: "228px",
        height: "228px",
        marginBottom: "10px"
    },
}));